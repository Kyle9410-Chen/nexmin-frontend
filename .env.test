# Committed so `pnpm test` works without a local .env — vitest runs in mode "test".
VITE_BACKEND_BASE_URL=https://api.example.test
# Tests drive MSW through src/mocks/server.ts directly, not the worker.
VITE_ENABLE_MOCKS=false
